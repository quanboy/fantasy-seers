version: '3.8'

services:

  postgres:
    image: postgres:15-alpine
    container_name: fantasy-seers-db
    environment:
      POSTGRES_DB: fantasyseers_db
      POSTGRES_USER: fantasyseers
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set (see .env.example)}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fantasyseers -d fantasyseers_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: fantasy-seers-backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/fantasyseers_db
      SPRING_DATASOURCE_USERNAME: fantasyseers
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set (see .env.example)}
      SPRING_JPA_HIBERNATE_DDL_AUTO: validate
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be set to a random string of at least 48 bytes (see .env.example)}
      JWT_EXPIRATION_MS: 86400000
      REGISTRATION_INVITE_CODE: ${REGISTRATION_INVITE_CODE:-}
      # Set in Railway — leave empty for local dev
      SENTRY_DSN: ${SENTRY_DSN:-}
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend/src:/app/src

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: fantasy-seers-frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  postgres_data:
